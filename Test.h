#ifndef TEST_H
#define TEST_H

#include <iostream>
#include "Data.h"
#include "Sort.h"
#include "Input.h"

void performTesting(const int max_elements, bool full_info_mode = true, std::ostream& stream = std::cout);

#endif 